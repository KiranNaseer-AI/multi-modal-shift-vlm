# PLACEHOLDER -- REPLACE BEFORE FINAL PUSH

The DAMF_TMM.tex / references.bib / figures/ in this folder are
NOT the current version of the paper. They predate several fixes
made directly in Overleaf and never synced back:

- Removal of the RSICD R_t method-comparison figure (page-limit
  and n=2/n=3 caption mismatch fix)
- Strengthened CIDEr appendix note (cross-dataset consistency
  argument added after this snapshot)
- New Appendix B: BERTScore semantic evaluation
- New Appendix: effective-update and parameter-grouping
  sensitivity analysis
- Several checklist-item propagation fixes (parameter
  breakdown table, correlation-detail relocation, validation-loss
  table, version-specification sentence)

Before pushing this repository as final: export your current
Overleaf project (.tex, .bib, figures/) and overwrite the three
items in this folder, then delete this placeholder file.
